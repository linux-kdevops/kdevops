---
- name: Install libvirt / kvm dependencies
  become: true
  become_method: sudo
  yum:
    name:
      - libvirt
      - libvirt-daemon
      - qemu
      - qemu-kvm
      - qemu-system-x86
    state: present

- name: Enable libvirtd
  become: true
  become_method: sudo
  systemd:
    name: libvirtd
    enabled: true
    masked: false

- name: Make sure libvirtd is running
  become: true
  become_method: sudo
  systemd:
    state: started
    name: libvirtd
