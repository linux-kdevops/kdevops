---
# Default values for monitoring role

# Enable monitoring services
enable_monitoring: false

# Enable developmental statistics
monitor_developmental_stats: false

# Enable folio migration monitoring
monitor_folio_migration: false

# Folio migration monitoring interval in seconds
monitor_folio_migration_interval: 60

# Enable system_metrics monitoring (sysstat sadc time series)
monitor_system_metrics: false

# system_metrics sadc sample interval in seconds
monitor_system_metrics_interval: 5

# Enable cpu_governor snapshot at workflow start and end
monitor_cpu_governor: false

# Enable block-layer + NVMe sysfs snapshot at workflow start and end
monitor_blockdev: false

# Snapshot-style monitors. The MONITOR_SNAPSHOTS_ALL Kconfig knob
# default-y's all of these via Kconfig's `default y if`, but the
# corresponding ansible variables still need their own defaults so
# extra_vars resolution doesn't trip on undefined references when
# Kconfig hasn't been re-rendered.
monitor_host_info: false
monitor_boot_params: false
monitor_cpu_features: false
monitor_clocksource: false
monitor_vm_tuning: false
monitor_lsm: false
monitor_dmi: false

# Base path to store monitoring results on the control host
# Workflows can override by setting monitoring_results_base_path
monitoring_results_base_path: "{{ topdir_path }}/workflows/fstests/results/monitoring"
