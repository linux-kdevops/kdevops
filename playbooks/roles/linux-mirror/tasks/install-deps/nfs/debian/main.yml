---
# SPDX-License-Identifier: copyleft-next-0.3.1
- name: Install NFS server packages on Debian/Ubuntu
  become: true
  ansible.builtin.package:
    name:
      - rpcbind
      - nfs-kernel-server
    state: present
  tags: ["mirror"]
