---
# SPDX-License-Identifier: copyleft-next-0.3.1
- name: Install partition creation base tools
  become: true
  become_method: sudo
  ansible.builtin.apt:
    name:
      - btrfs-progs
      - e2fsprogs
      - xfsprogs
    update_cache: true
