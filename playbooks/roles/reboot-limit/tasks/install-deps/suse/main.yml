---
- name: Install kexec-tools and dependencies for reboot-limit on SUSE
  become: yes
  become_method: sudo
  ansible.builtin.zypper:
    name:
      - kexec-tools
    state: present
    update_cache: yes
  tags: [ 'install', 'kexec' ]
