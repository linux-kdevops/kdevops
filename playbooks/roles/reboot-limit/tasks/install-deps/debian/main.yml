---
- name: Install kexec-tools and dependencies for reboot-limit on Debian
  become: true
  become_method: sudo
  ansible.builtin.apt:
    name:
      - kexec-tools
    state: present
    update_cache: true
  tags: ["install", "kexec"]
