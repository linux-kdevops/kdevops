---
- name: Install systemd-timesyncd
  become: true
  become_method: sudo
  ansible.builtin.apt:
    name:
      - systemd-timesyncd
  when:
    - devconfig_enable_systemd_timesyncd|bool
  tags: timesyncd
