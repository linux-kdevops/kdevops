---
- name: Install build dependencies for ndctl
  become: true
  become_method: sudo
  ansible.builtin.dnf:
    update_cache: true
    name: "{{ packages }}"
  vars:
    packages:
      - git-core
      - meson
      - cmake
      - gcc
      - pkgconf-pkg-config
      - kmod-devel
      - systemd-devel
      - uuid-devel
      - libuuid-devel
      - json-c-devel
      - rubygem-asciidoctor
      - keyutils-libs-devel
      - iniparser-devel
      - bash-completion
      - jq
      - libtraceevent-devel
      - libtracefs-devel
